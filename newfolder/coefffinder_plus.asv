function [xval] = coefffinder_plus(theta, r)
%UNTITLED3 Summary of this function goes here
%   Detailed explanation goes here

syms X

sj = theta(1); sa = theta(2); g = theta(3); f = theta(4);

Rzerotwo = (f*sj*g)/((1-sa)*(1-sj*(1-g)));
Om = (1-(1-r)*Rzerotwo)/r;

a = -(Om*sa*sj*g + f*sj*g);
b = (Om*g*sj + Om*sa*sj);
c = (Om*sj + Om*sa);

P = a*X^3 + b*X^2 - c*X + Om == 0;

xval = vpa(solve(P, X, "Real",true, ))

end